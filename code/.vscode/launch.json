{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "make",
            "cwd": "${workspaceRoot}",
            "executable": "./bin/main",
            "name": "Debug STM32L4",
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "openocd",
            "configFiles": [
                "openocd.cfg"
            ],
            "svdFile": "${workspaceFolder}/STM32L4x5.svd"
        }
    ],
}
